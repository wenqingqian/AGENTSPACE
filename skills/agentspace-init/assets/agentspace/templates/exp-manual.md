# exp_{{ID}} {{TITLE}}

> 状态: todo
> 创建: {{DATE}}
> 索引: 见 exp.md 与 exp/index.md (由 scripts/ 维护)
> 配置: examples/exp_spec/exp_{{ID}}/ (由 new-exp.sh 预创建)
> 数据: exp/exp_data/exp_{{ID}}/ (完整实验记录, 本机全量, 不入 git)

## 实验问题与范围

<!-- 要回答的问题(一句话) + 固定的测试范围(数据/模型/环境边界); 明确写出范围外的内容。
     一个 exp = 一个大目标下的一组实验轮次: 本节写目标级的问题与范围, 各轮细节留在"轮次"节。
     范围未固定前不开跑(agentspace-better-exp 对齐输出落本节)。 -->

## 假设与预期

<!-- 目标级预期结果 + 成功判据 + 证伪判据(开跑前写定); 终止条件与预算(时长/成本上限)。 -->

## 方案与配置

<!-- 目标级方法概述: 基线与对照(公平性: 同数据/同环境/同口径, 单变量) / 迭代数与 warmup /
     计时口径(只计核心目标, 无外部开销) / 随机种子与方差报告 / 指标定义(公式/单位/统计口径) /
     复现命令。配置文件写入 examples/exp_spec/exp_{{ID}}/ 并逐个登记(文件名 + 一句话说明;
     多轮实验建议按轮命名, 如 run1-baseline.yaml、run2-lr-sweep.yaml)。 -->

## 轮次

<!-- 一个 exp = 一个大目标: 后续小轮次(新参数轮/补测/增量结果)追加在本节, 不新开 exp、
     无需再过登记确认(agent 报备即可); 归属不清(后续轮次还是新目标)问用户。
     每轮一个三级标题, 按时间追加, 不改历史轮次:
       ### run-1 ({{DATE}}) — 一句话本轮问题
       - 变更: 相对上一轮改了什么(单变量); 首轮写主要设置
       - 配置: 本轮配置文件名(examples/exp_spec/exp_{{ID}}/ 内)
       - 结果: 一句话本轮结论
       - 数据: exp/exp_data/exp_{{ID}}/ 下本轮产物(建议按轮建子目录 run1/)
     目标级五轴设计对齐只在登记时做一次; 后续轮次做增量核对(改了什么、是否仍单变量)。 -->

## 关联

<!-- 关联 plan / iteration(可无); 测试用的关键仓库 commit 点在关闭时经 --commit 落索引。 -->

## 结果

<!-- 一句话结论; 关闭 exp 前必填 -->

## data 产物清单

<!-- exp_data/exp_{{ID}}/ 下各文件是什么、怎么产生的(多轮按轮说明)。
     exp_data 为本机全量记录(不入 git), 关联 iteration 的 data/ 产物复制一份至此;
     持久结论走本节与 notes, 不依赖 exp_data 的可迁移性。 -->

## 日志

<!-- append-only: 每次工作追加一行, 不修改历史行。 -->
- {{DATE}} 创建
