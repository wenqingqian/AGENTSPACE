# iteration_{{ID}} {{TITLE}}

> plan: {{PLAN_ID}}
> 状态: 进行中
> 创建: {{DATE}}

## 当前状态 · 下一步

<!-- 会话续接块: 每次结束工作前更新"干到哪 / 卡在哪 / 下一步做什么"。
     这是新会话恢复上下文的入口; iteration 关闭后本节冻结。 -->

## 目标

<!-- 这一轮要验证 / 完成什么 -->

## 改动摘要

<!-- 相对上一轮改了什么(代码 / 配置 / 数据 / 环境) -->

## 环境

<!-- 引用 tests.md 的环境条目; 记录宿主仓库状态: git rev-parse HEAD
     (宿主工作区有未提交改动时, 存 diff 到 data/: git -C <宿主> diff > data/code.diff) -->

## 结果

<!-- 指标 / 结论; 关闭 iteration 前必填 -->

## data 产物清单

<!-- data/ 下各文件是什么、怎么产生的。
     data/ 全量本地保存, 已被 gitignore 屏蔽, 不入 git -->

## 日志

<!-- append-only: 每次工作追加一行, 不修改历史行 -->
- {{DATE}} 创建
