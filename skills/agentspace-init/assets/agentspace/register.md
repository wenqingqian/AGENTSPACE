# Register 模块注册表

> 按需扩展模块的登记处。注册新模块前**必须先与用户确认**, 然后用 `scripts/register-module.sh <name> "用途"` 注册。
> 每个模块 = `<name>.md`(入口索引) + `<name>/`(内容), 模板 templates/module-entry.md。
> 示例: examples.md + examples/ 存放固定测试配置。

## 已注册模块

| 模块 | 用途 | 入口 | 注册日期 |
| --- | --- | --- | --- |
